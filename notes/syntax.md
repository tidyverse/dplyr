# Syntax ideas

    _summarise()
    _mutate()
    _arrange()
    _subset()

    _by(grouping thing, {
      _summarise()
      _mutate()
      _arrange()
      _subset()
    })

    _apply(general function)
    _collapse() ?

