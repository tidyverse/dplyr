> tibble(x = 1) %>% summarise(res = across(is.numeric, 42))
Error: `summarise()` argument `res` errored.
i `res` is `across(is.numeric, 42)`.
x `.fns` must be NULL, a function, a formula, or a list of functions/formulas

