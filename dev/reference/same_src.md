# Figure out if two sources are the same (or two tbl have the same source)

Figure out if two sources are the same (or two tbl have the same source)

## Usage

``` r
same_src(x, y)
```

## Arguments

- x, y:

  src or tbls to test

## Value

a logical flag
