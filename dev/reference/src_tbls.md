# List all tbls provided by a source.

This is a generic method which individual src's will provide methods
for. Most methods will not be documented because it's usually pretty
obvious what possible results will be.

## Usage

``` r
src_tbls(x, ...)
```

## Arguments

- x:

  a data src.

- ...:

  other arguments passed on to the individual methods.
